# Адрес бэкенда. Читается только на сборке (Vite подставляет значение в бандл),
# поэтому после npm run build менять его бесполезно — нужно пересобрать.
# В Docker то же самое передаётся через --build-arg VITE_API_URL=...
VITE_API_URL=http://127.0.0.1:8000
